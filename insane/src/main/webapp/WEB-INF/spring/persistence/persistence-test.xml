<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd"
       profile="test">

    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <!--
            By default, closing the last connection to a database closes the database.
            For an in-memory database, this means the content is lost.
            To keep the database open, DB_CLOSE_DELAY=-1 is required
        -->
        <property firstName="driverClassName" value="org.h2.Driver"/>
        <property firstName="url" value="jdbc:h2:mem:javabank;DB_CLOSE_DELAY=-1"/>
        <property firstName="username" value="sa"/>
        <property firstName="password" value=""/>
    </bean>

    <bean id="emFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">

        <property firstName="dataSource" ref="dataSource"/>
        <property firstName="packagesToScan" value="org.academiadecodigo.codezillas.insane.persistence.model"/>
        <property firstName="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>
        </property>
        <property firstName="jpaProperties">
            <map>

                <!-- Uncomment for debug -->
                <!--<entry key="hibernate.show_sql" value="true" />-->

                <entry key="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>

                <!-- Create database schema and populate with test data -->
                <entry key="hibernate.hbm2ddl.auto" value="create"/>
                <entry key="hibernate.hbm2ddl.import_files" value="db/test-data.sql"/>
                <entry key="hibernate.hbm2ddl.import_files_sql_extractor"
                       value="org.hibernate.tool.hbm2ddl.MultipleLinesSqlCommandExtractor"/>

            </map>
        </property>
    </bean>


</beans>
